pipeline {
    agent any

    stages {
        stage('Hello World') {
            steps {
                sh """
                    echo 'Hello, World!'
                """
            }
        }

        stage('Archive File') {
            steps {
                    archiveArtifacts artifacts: 'Jenkinsfile', fingerprint: true
            }
        }
    }
}
